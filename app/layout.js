import './globals.css';

export const metadata = { title: 'Tugas Pintar', description: 'Aplikasi to-do list ringkas dan moden.' };

export default function RootLayout({ children }) {
  return <html lang="ms"><body>{children}</body></html>;
}
